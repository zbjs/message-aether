```sh
yarn add amqplib axios bcryptjs cors dotenv express express-async-errors joi jsonwebtoken mongoose multer nodemailer pg swagger-ui-express uuid yup nodemailer-mailgun-transport nodemailer-sendgrid-transport superagent supertest

```

```sh
yarn add -D supertest nodemon eslint eslint-config-standard eslint-plugin-import eslint-plugin-node eslint-plugin-promise eslint-plugin-standard

```
